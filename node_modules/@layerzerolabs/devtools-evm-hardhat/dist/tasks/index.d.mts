import '../type-extensions.mjs';
import { LogLevel } from '@layerzerolabs/io-devtools';
import { OmniTransaction, OmniSignerFactory } from '@layerzerolabs/devtools';
import '@layerzerolabs/lz-definitions';
import '@safe-global/protocol-kit';
import '@layerzerolabs/devtools-evm';

interface SimulationLogsTaskArgs {
    logLevel?: LogLevel;
}

interface SimulationStartTaskArgs {
    logLevel?: LogLevel;
    networks?: string[];
    daemon?: boolean;
}

interface SimulationStopTaskArgs {
    logLevel?: LogLevel;
}

interface SignAndSendTaskArgs {
    ci?: boolean;
    transactions: OmniTransaction[];
    createSigner: OmniSignerFactory;
}

export type { SignAndSendTaskArgs, SimulationLogsTaskArgs, SimulationStartTaskArgs, SimulationStopTaskArgs };
